#include "CommandTopic.hpp"

CommandTopic::CommandTopic() {}
CommandTopic::~CommandTopic() {}
void CommandTopic::execute(int &clientSocket,
                           Client *client,
                           Server *server,
                           std::string payload) {}
